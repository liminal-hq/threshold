# Threshold Site

This is the landing page for Threshold, a flexible alarm and timer application.

## Structure
- `index.html`: The single-file landing page.
- `assets/`: Contains images and logos used on the site.

## Deployment
This site is deployed to GitHub Pages via a GitHub Action workflow located in `.github/workflows/deploy-site.yml`.
