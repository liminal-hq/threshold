{
	"name": "window-alarm-monorepo",
	"version": "1.0.0",
	"private": true,
	"license": "UNLICENSED",
	"scripts": {
		"test": "pnpm -r test",
		"dev:desktop": "pnpm --filter window-alarm tauri dev",
		"dev:android": "pnpm --filter window-alarm tauri android dev",
		"format": "prettier --write ."
	},
	"packageManager": "pnpm@10.28.0",
	"devDependencies": {
		"prettier": "^3.8.0"
	}
}
