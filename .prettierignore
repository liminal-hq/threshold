dist
node_modules
coverage
vendor
target

# Generated by cargo test (alarm::models::ts_bindings) -- its own
# formatting must match exactly, or the drift-check test fails.
apps/threshold/src/types/alarm.ts

# Regenerated by each plugin's build.rs on every `cargo build`/`cargo test`
# (tauri_plugin::Builder::build()) -- reformatting these just fights the
# next build, since the generator doesn't know about prettier's style.
plugins/*/permissions/autogenerated
plugins/*/permissions/schemas/schema.json

# Tauri-generated Android/schema scaffold -- CLAUDE.md: "don't hand-edit".
apps/threshold/src-tauri/gen
